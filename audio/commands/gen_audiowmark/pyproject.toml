[project]
dependencies = [
  "common",
  "pyyaml>=6.0.3",
]
name = "gen-audiowmark"
requires-python = ">=3.14"
version = "0.1.0"

[dependency-groups]
dev = []

[tool.uv]
exclude-newer = "7 days"

[tool.uv.sources]
common = { path = "../../../common", editable = true }
