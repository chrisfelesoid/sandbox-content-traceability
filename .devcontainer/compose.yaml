services:
  sandbox-content-traceability:
    build:
      context: ..
      dockerfile: ./.devcontainer/Dockerfile
      target: dev
    platform: linux/amd64
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    tty: true
    volumes:
      - ..:/workspace
